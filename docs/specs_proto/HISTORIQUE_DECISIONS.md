# Historique des décisions validées

> **Dernière mise à jour** : 29/04/2026 PM tardif (Étape 7 multi-tenant **TERMINÉE — 22/22 caches migrés (100%)** + cleanup BG users inactifs > 30j en place + 11 commits atomiques en production OVH sans perte ni régression)

Extrait de CLAUDE.md — tableau chronologique complet (15/03 → 06/04/2026)

| Date | Décision | Détail |
|------|----------|--------|
| 15/03 | EasyMail v1 | Version stable, Haiku, recherche < 2s, génération < 3s |
| 15/03 | Modal refonte | 3 boutons Windows, drag, minimize/maximize, Cc, signature |
| 15/03 | Niveaux R/S/H | 3 niveaux + auto-détection mots sensibles |
| 15/03 | Pre-fetch universel | Toujours niveau 3 en cache, troncature à la génération |
| 15/03 | Onboarding | 80 → 100 mails, popup non-bloquant |
| 15/03 | Page Profil | Métriques, scoring, importance par défaut |
| 15/03 | Dates intelligentes | Heure si aujourd'hui, date sinon |
| 16/03 | Suppression scan dossiers | Tout le code folder index supprimé (simplification) |
| 17/03 | Haiku → Sonnet | Migration validée pour qualité x10 |
| 17/03 | System prompt simplifié | 89 → 25 lignes |
| 17/03 | Forward intelligent | Double contexte (expéditeur + destinataire), garde bouton |
| 17/03 | Autocomplete contacts | Sur tous les champs email |
| 17/03 | Onboarding 300 mails | 100 → 300 envoyés + 300 reçus (validé) |
| 17/03 | PJ analyse checkboxes | Sélection granulaire, spinner, 2 temps |
| 17/03 | Champs éditables | À/Cc/Objet modifiables dans tous les modes |
| 18/03 | Opus pour H | Prévu mais PAS encore implémenté, garder Sonnet partout |
| 18/03 | Champ forward-to visible | Champ destinataire dans le panneau droit en mode forward + garde bouton + autocomplete |
| 18/03 | Gestion des échéances | Détection IA (Haiku), page dédiée, pop-up proactive, relance pré-remplie, Bloc F, scan post-envoi + onboarding |
| 18/03 | Correspondants → Contacts | Renommage dans toute l'UI |
| 18/03 | Header redesign | Logo gauche, boutons (Contacts, Échéances, Profil) alignés à droite |
| 18/03 | Refonte prompt engineering | Méthode imitation 3 étapes, ordre D→B→A→C→D2→E, instructions simplifiées, D2 avec catégories |
| 18/03 | Optimisation latence prefetch | Prefetch 2 phases (meta < 1s → bodies background), max_read 15→6 (puis 6→4 pour H), enrich_bodies découplé |
| 21/03 | Spéculation streaming | Buffer streaming en arrière-plan, bouton ≤4s, suppression Plan B + spec-instant (~200 lignes), 1 seul path SSE, fix bug importance dans spéculatif |
| 21/03 | Phase 1 queue FAST | Phase 1 AB passe sur queue COM interactive (priority=0), jamais bloquée par le BG |
| 21/03 | Timer 4s sans condition | Bouton actif à 4s max, indépendamment de l'état Phase 1/2/Claude |
| 21/03 | Fallback 0-chunks | Si buffer vide au clic → génération streaming normale + message "contexte réduit" + anti double-génération (buffer.done + pop cache + double check) |
| 21/03 | Fix PJ spéculatif | Skip popup PJ en mode spéculatif sans brief (évite blocage modal) |
| 21/03 | Nettoyage cache 2 clés | Suppression systématique email_id + full_id dans _speculative_cache |
| 21/03 | 4 audits code | 5 anomalies corrigées (double API, fuite mémoire, partial_context flag, onclick 🔄, check ordre) |
| 23/03 | Chantier 1 : Refonte style profile | 3 sections (A paires, B règles, C mails), 80 mails échantillonnés, 55 paires, placeholders anti-hallucination |
| 23/03 | Chantier 2 : Prompt WOW | 3 phases (Comprendre→Rédiger→Vérifier), hiérarchie 8 priorités, "comme moi mais en mieux" |
| 23/03 | Chantier 3 : Scoring similarité | Heuristique mots-clés pour trier bloc B par type de situation |
| 23/03 | Chantier 4 : D2 enrichi | Micro-analyse Claude du diff post-envoi en background |
| 23/03 | Onboarding 2-3 min | Lazy loading profils contacts, 300 envoyés + 500 reçus, barre progression, bouton stopper |
| 23/03 | Bloc C obligatoire S/H | S et H attendent bodies + C (timeout 25s) avant de générer, C meta sur queue FAST |
| 23/03 | Fix prefetch C critique | Thread C lancé au cache miss (pas au cache hit), corrige C=0 systématique |
| 23/03 | Images inline asynchrones | Placeholders gris + chargement background, plus de blocage 120s |
| 23/03 | Anti-hallucination renforcé | 10 catégories données factuelles, créneaux → [À CONFIRMER], décisions → [DÉCISION À PRENDRE] |
| 23/03 | 6 audits code | ~12 bugs corrigés (Events set/clear, DEBUG prints, cache trim, prefetch C inversé) |
| 25/03 | Pré-scan échéances | Scan Claude pendant la relecture, post-envoi réutilise le résultat, polling 6×500ms (3s max), cas ultra-rapide → bandeau inbox |
| 25/03 | Classement mails dossiers Outlook | 4 scénarios (réponse/transfert/nouveau/classer sans répondre), suggestion hybride (règle 3+ → IA fallback), popup post-envoi chaînée après échéances, apprentissage contact→dossier, move reçu + copy envoyé |
| 26/03 | Retry API Claude | 3 tentatives auto sur erreurs Overloaded (backoff 2/4s), messages user-friendly |
| 26/03 | Historique classement par sujet | Table classification enrichie : contact + mots-clés sujet → dossier. Règle auto vérifie cohérence du sujet avant de proposer |
| 26/03 | Classement PJ explorateur Windows | Scan arborescence Windows au warmup, suggestion IA dossier, popup post-envoi, copie + renommage intelligent |
| 26/03 | Échéance sans date | Popup 3 boutons (Ignorer / OK noté / Ajouter échéance) + sélecteur délai (1-30j + personnalisé) + bouton retour |
| 26/03 | Arborescence +/- dossiers | Toggle expand/collapse avec flèches (comme Outlook), tri alphabétique, scroll vers dossier sélectionné, max-height 450px |
| 26/03 | Affichage À/Cc dans header mail | Champs À et Cc affichés dans le header du mail reçu (identification copie) |
| 26/03 | Popup classement instantanée | Spinner "Analyse en cours" immédiat, résultat chargé en arrière-plan |
| 26/03 | Suppression inline | Tooltip de confirmation à côté de la corbeille (plus de confirm() central) |
| 26/03 | Warmup dossiers + Windows | Pré-chargement Outlook folders + Windows folders au démarrage |
| 26/03 | Pré-chargement mail voisin | 2 au-dessus + 2 en-dessous pré-chargés après le spéculatif du mail courant |
| 26/03 | Inbox instantanée | Cache toujours affiché (même périmé) + refresh BG, mail classé retiré du cache |
| 26/03 | Optimisation post-envoi | 7 daemons regroupés en 3 threads (popups / apprentissage / contact) |
| 26/03 | move_to_folder sur BG | Classement mail ne bloque plus la queue FAST |
| 26/03 | suggest_folder sans COM | Cache folders vérifié en mémoire, attente warmup 15s max, fallback COM |
| 26/03 | Fallback DB contexte B | Si prefetch AB ne trouve pas d'historique, fallback sur threads DB |
| 26/03 | 7 passes d'audit | 46 bugs corrigés (crashs, fuites mémoire, fetch sans catch, escaping, code mort, incohérences) |
| 27/03 | Classement mail amélioré | Body snippet 500→1500 chars, tri naturel dossiers, scroll auto vers dossier suggéré, purge fausses règles |
| 27/03 | Gardes qualité renforcées | Anti-contamination registre, garde post-gen (registre + greeting + closing vs profil D), anglicisme bloqué (Salut/Hello/Hi interdit pour nouveaux contacts) |
| 27/03 | OCR PDF scannés | Claude Vision multi-images (max 5 pages, DPI 150), fallback si PyPDF2 retourne texte vide |
| 27/03 | UX modal/popups | Bouton corbeille email_detail + échéances, fusion Ignorer/OK noté, bandeau inbox intelligent (relances + dépassées), header sticky, format dates "mercredi 8 avril" |
| 27/03 | Recalibrage | Bouton "Recalibrer EasyMail" (profil + contacts), bouton "Tout recalibrer" contacts, recalibrage individuel par fiche, seuil re-analyse 5→3 mails |
| 27/03 | Optimisation queues COM | Analyse globale 40+ appels, 4 changements de priorité sûrs, matrice documentée |
| 27/03 | Liens externes + iframe | allow-scripts + allow-popups, liens cliquables dans les mails |
| 27/03 | current_draft préservé | Brouillon capturé AVANT vidage éditeur, injecté dans le brief de régénération |
| 27/03 | Forward PJ sans doublon | Filtre isOriginal en mode forward (msg.Forward() inclut déjà les PJ) |
| 27/03 | Sécurité | Path traversal normcase Windows, CSP meta dans iframe, secret_key persistante |
| 27/03 | 4 passes d'audit complètes | 52 bugs trouvés, 49 corrigés (4 critiques, 8 hautes, 15 moyennes, 22 faibles). 3 restants = race conditions threading |
| 27/03 | Landing page | One-page marketing EasyMail créée |
| 27/03 | 30 scénarios de test | 15 normaux + 15 avancés (cas limites, stress, edge cases) |
| 28/03 | Cache body DB | Pré-chargement 15 mails inbox en DB au warmup, lecture DB 50ms au lieu de COM 4-13s. Warmup BG progressif toute l'inbox |
| 28/03 | Tests 30 mails complets | 30 scénarios exécutés : 0 crash, 1 bug critique (confusion interlocuteur corrigé), 5 ajustements |
| 28/03 | Fix confusion interlocuteur | Garde greeting vérifie from_name du mail vs greeting généré |
| 28/03 | Classement contenu > objet | Règle 7 : si contenu diverge de l'objet, le contenu prime (ex: objet=Asturia, contenu=Greenpark) |
| 28/03 | Échéances filtrées | Dates passées rejetées (prompt + code). Échéances induites sans marqueur temporel ignorées |
| 28/03 | Polling échéances 3s→6s | 6→12 tentatives de polling pour laisser le scan finir |
| 28/03 | Erreur envoi claire | Frontend lit data.error du JSON, messages Outlook traduits (adresses non résolvables, etc.) |
| 28/03 | Nettoyage markdown | Double filtre backend (re.sub) + frontend (replace) pour supprimer ** et • des réponses |
| 28/03 | Popup post-envoi draggable | Popup "Mail envoyé" déplaçable pour accéder au mail en dessous |
| 28/03 | Inbox auto-refresh démarrage | Force refresh Outlook au premier chargement (plus de décalage inbox) |
| 29/03 | 60 scénarios de test | 15 normaux + 15 avancés + 15 expert (angles morts) + 15 diaboliques (durcissement + échéances liées) |
| 29/03 | Audit passe 1 | 38 bugs identifiés, 17 corrigés : 2 critiques (path traversal, XSS), 7 hauts (juin/juillet, échéances vides, tutoiement écrasé, refine sans cache, suggest_folder null, markdown new_mail, fuite OCR handle, garde anti-contamination contexte C) |
| 29/03 | Audit passe 2 | 21 restants ré-examinés, 12 corrigés : rate-limit lock, echeance_exists stop words, register word boundary, contact display service emails, PJ cache cleanup, SearchFuture cleanup, save_contact_profile transaction, var confirm renommé (x4), code mort importance fixé, modal relance fermable, closeModal garde onboarding |
| 29/03 | Bilan audit total | 38 bugs → 29 corrigés, 7 restants (ops dict/bool atomiques CPython GIL, mono-user), 2 faux positifs |
| 29/03 | Pré-extraction PJ PDF background | Thread BG à l'ouverture du mail : PyPDF2 + OCR Vision si scanné, résultat en cache → /api/extract_attachments retourne instantanément. Attend _bodies_enriched + _c_context_ready avant COM (zéro impact spéculatif) |
| 29/03 | suggest_folder pré-calculé analysé | Analyse d'impact négative : 80% des cas utilisent des règles DB (pas d'IA), risque rate-limit Claude, body envoyé pas dispo à l'ouverture. Non implémenté — le post-envoi en daemon est le bon compromis |
| 30/03 | **60 scénarios testés** | MAIL 31-60 : expert (31-45) + diaboliques (46-60). Tous validés. Injection HTML bloquée, anti-hallucination, brief contradictoire, forward restrictif, thread profond, OCR réel, signatures invasives, menaces juridiques |
| 30/03 | Installeur beta-testeurs | install.bat (scan système + install auto Python/Git/Outlook), phase2_setup.py (pip, COM, config, Git clone, raccourci bureau), build_zip.bat (packaging ZIP avec clé API injectée) |
| 30/03 | MAJ automatiques Git | start.bat : git pull silencieux au démarrage. app.py : thread BG git fetch toutes les 5min, bandeau live "Yvan vient d'améliorer EasyMail", restart auto au clic |
| 30/03 | Règle anti-décision financière | System prompt : interdit de proposer montants, répartitions, acceptations/refus sans brief. Placeholders [DÉCISION À PRENDRE] obligatoires |
| 30/03 | Règle anti-négation factuelle | "Aucun virement effectué" = invention. Utiliser [À VÉRIFIER] |
| 30/03 | Règle données sensibles | Interdit [RIB À TRANSMETTRE]. Refus poli pour RIB/IBAN/mots de passe |
| 30/03 | Règle confidentialité Reply All | Ne pas inclure montants/quotes-parts personnels dans un reply all |
| 30/03 | Détection langue auto | Répondre dans la langue du mail reçu (anglais → anglais, etc.) |
| 30/03 | Scan échéances mail reçu | Pre-scan + post-send scannent le mail reçu ET la réponse (avant : réponse seule) |
| 30/03 | Fix confirmation échéances new_mail | _nmConfirmEch faisait un POST vers /api/echeances/confirm (manquant avant) |
| 30/03 | OCR new_mail | Fallback Claude Vision ajouté dans /api/extract_file_text (avant : uniquement email_detail) |
| 30/03 | Correction dates proximité | _validate_echeance_date : match par proximité (delta jours) au lieu de dernière date du texte |
| 30/03 | Refresh inbox FAST | get_inbox_emails BG passe sur priority=0 (FAST) — plus bloqué derrière AdvancedSearch |
| 30/03 | Overlay "Envoi en cours" | sending-overlay bloquant dès le clic "Relire et envoyer", caché par la popup suivante |
| 30/03 | Input file hors modale | <input type="file"> déplacé au body level (compatibilité Chrome/OneDrive) |
| 30/03 | Avertissement PDF tronqué | Bandeau orange "⚠️ 10 pages sur 42 analysées" dans la popup PJ |
| 30/03 | UX échéances | Recherche, bouton Modifier (popup +/-), objet dans le header, "Mail envoyé il y a X jours", "✉️ Voir le mail original" |
| 30/03 | UX footer réponse | "Essayer une autre réponse" + "↩ Version précédente" groupés après "Plus travaillé". "Plus rapide" → "Plus court" |
| 30/03 | Fix ReportItem | Guard Class!=43 dans get_email_by_id → return None (évite crash sur notifications de non-remise) |
| 30/03 | Fix os.execv Windows | subprocess.Popen + os._exit(0) au lieu de os.execv (incompatible Windows) |
| 30/03 | Fix branche git auto | Détection auto main/master pour les MAJ. Timeouts ajoutés sur tous les subprocess git |
| 30/03 | **Audit complet 2 passes** | Passe 1 : 9 bugs trouvés et corrigés (2 critiques, 5 moyens, 2 bas). Passe 2 : 0 bug résiduel. 10 agents, 8000+ lignes auditées |
| 30/03 | **Statut : PRÊT POUR BÊTA** | 60 tests validés, 2 audits passés, installeur prêt, MAJ auto Git, email type avec NDA rédigé |
| 03/04 | **Scoring rédactionnel complet** | Phase 1 (scoring onboarding 5 critères + 3 descripteurs), Phase 2 (prompt level-aware + plancher 70 + scoring×contact), Phase 3 (évolution score + barème + hystérésis ±3), Phase 4 (convergence ±3 sur 5 cycles), enrichissement 4 canaux, Point A (recalibrage manuel conserve le score), multi-classification D2 (AMELIORATION > DEGRADATION > STYLE) |
| 03/04 | Toolbar enrichie | 16 polices, taille libre (combobox custom), listes puces/numérotées, surligneur 15 couleurs |
| 06/04 | **11 optimisations codées** | Git fetch 2h, recalibrage adaptatif 10/20/50, contacts schedule fixe, D2 fusionné recalibrage, échéances pré-filtre heuristique (regex dates+engagement), smart spéculatif (5 filtres), cache brouillon 24h localStorage, templates intelligents (45 templates auto-detection), classification top 3 + thread matching + domaine + cross-contact + momentum, cache contextuel C 24h, nettoyage email body |
| 06/04 | Templates intelligents | 45 templates fixes tu/vous, détection auto (mail reçu court < 15 mots + brief court < 20 chars), 10 gardes de sécurité, assemblage greeting+corps+closing+signature |
| 06/04 | Classification enrichie top 3 | Thread matching (même fil = même dossier), Tier 1 bis body keywords, règles domaine (hors domaines publics), sujet cross-contact (3+ contacts), momentum (30 min), popup 3 radio buttons + arborescence |
| 06/04 | Smart spéculatif | 5 filtres (mail >7j, traité, noreply, <10 chars, CC), cache brouillon 24h avec invalidation |
| 06/04 | Recalibrage adaptatif | <30 corrections → tous les 10, 30+ → tous les 20, convergé → tous les 50 envois |
| 06/04 | D2 fusionné | Micro-analyse D2 intégrée dans le recalibrage (0 appel individuel, classifications dans <<<CLASSIFICATIONS>>>) |
| 06/04 | Échéances pré-filtre | Regex dates futures + mots-clés engagement, skip 85% des scans IA inutiles |
| 06/04 | Fix brief | Brief guide le contenu pas le style, ne jamais reprendre mot pour mot |
| 06/04 | Fix forward | Bouton Générer actif dès destinataire rempli, logique PJ maintenue |
| 06/04 | **Audit 3 passes** | Passe 1 : 14 bugs (4 corrigés critiques/hauts). Passe 2 : 0 critique. Passe 3 : 0 régression. Code prêt pour bêta |
| 18/04 | **Cache unifié `_reply_cache`** | Fusion `_preemptive_cache` + ex-cache brouillon en une seule structure. Absorbe spéculation (status=generated) et édition user (status=edited). Remplace l'ancien modèle 2 caches séparés. |
| 18/04 | **Purge événementielle pure + safety net 4 semaines** | Suppression complète des TTL (30 min spéculation, 24 h brouillon, 7 j brouillon). Purge uniquement sur événements : classify / send / delete / archive / reply-externe / cohesion-refresh. Safety net 28 j pour éviter croissance indéfinie. |
| 18/04 | **Smart Speculative : 5 + 1** | Ground truth du code proto : seulement 5 filtres implémentés (la spec en annonçait 6). Le filtre "open_count" (mail ouvert 2+ fois sans réponse) est **à créer** en V2, pas à porter. Motif `postmaster` conservé en plus des 5 motifs no-reply de la spec. |
| 18/04 | **Popup 4 modes (user × cache)** | Raffine la décision du 10/04 "popup à chaque démarrage". Matrice 2×2 : user activé/pas activé × cache chaud/froid. Popup toujours affichée (sert aussi de temporisateur pendant warmup). Définition "activé" = OAuth Microsoft + style_profile.txt + flag `user_activated=1`. |
| 18/04 | **Corrections Plan 3 (audit code)** | 2 divergences vs Plan 3 initial : `_windows_folders_cache` faux positif (absent V2, présent proto seulement — à porter maintenant pour classement auto PJ) et `_c_keyword_cache` faux négatif (présent V2 depuis 14/04 à [V2/app_plugin.py:391](../../V2/app_plugin.py#L391)). |
| 18/04 | **Régression brouillons perdue** | Code brouillon documenté 14/04 (routes `/api/save_draft`, `/api/get_draft`, persistance `drafts_v2.json`) disparu de V2 entre 17/04 (dernière écriture disque) et 18/04 (consolidation). Fichier `drafts_v2.json` toujours présent avec données. À restaurer depuis commit `25d4629` OU ré-implémenter dans cache unifié lors du Plan 2. |
| 25/04 | **Décision SaaS** | Installation locale (start.bat + ZIP) abandonnée. BoosterMail migre en SaaS : serveur OVH VPS France (12€/mois), OAuth2 Microsoft multi-tenant (déjà à 80% dans `auth_base.py`), plugin Outlook via AppSource, webhooks Graph API pour traitement nocturne, Stripe pour abonnements. Plan 5 phases dans l'ordre : 1→5→2→beta→4→3. Durée estimée : 4-5 jours de travail. Pas de dev externe nécessaire. SQLite conservé (PostgreSQL repoussé à +200 users). Companion et boostermail_service.py supprimés du plan. Doc : `docs/plans/PLAN_SAAS.md`. |
| 25/04 | **Développement V2 en parallèle du SaaS** | V2 continue d'évoluer en local (C:\EasyMail\V2\). Pas besoin de "finir V2" avant de migrer. Workflow : développement local → déploiement sur OVH au fil de l'eau. Les deux environnements coexistent. *(NB 27/04 PM : workflow révisé suite décision Yvan « OVH = source de vérité unique » — voir entrée 27/04 PM ci-dessous.)* |
| 25/04 | **Architecture multi-provider confirmée** | `ai_provider.py` (Claude/GPT/demain Mistral), `email_provider.py` (Graph/demain Gmail), `auth_base.py` (Microsoft/demain Google) — les trois abstractions sont déjà en place. Gmail et IA open source s'intègreront sans réécriture. |
| 25/04 | **AppSource — soumission immédiate** | Soumettre BoosterMail sur AppSource Microsoft dès maintenant (validation 4-8 semaines). En parallèle de tout le reste. Cible : 2 clics pour installer, disponible dans le store Outlook intégré. *(NB 27/04 : différé en attente décision MPN/entité éditrice — voir entrée 27/04 ci-dessous.)* |
| 25/04 | **LaunchEvent OnMessageCompose** | Changer `OnNewMessageCompose` → `OnMessageCompose` dans manifest.xml (1 mot). Intercepte automatiquement le clic "Répondre" → popup BoosterMail s'ouvre sans action de l'utilisateur. Combiné au traitement nocturne : réponse déjà prête à l'ouverture du mail. *(Réalisé 26/04 PM — voir entrée Étape 1.)* |
| 25/04 | **Pivot SaaS — BoosterMail hébergé (Phase 1 démarrée)** | Migration de l'installation locale (proto + V2 + start.bat + companion COM) vers SaaS hébergé sur **VPS OVH Gravelines** (4 vCores / 8 Go RAM / 160 Go SSD NVMe, ~22€/mois HT). Motivation : éliminer les frictions d'installation (versions Windows/Outlook variables, COM, OneDrive, droits admin). Architecture V2 déjà compatible : `auth_base.py` OAuth2 multi-provider à 80%, `ai_provider.py` abstraction multi-IA, `email_provider.py` abstraction multi-email. Domaine **`boostermail.ai`** acheté 25/04 (~80€/an). Coût fixe ~29€/mois HT (VPS 22€ + domaine 7€), rentable dès 2 clients à 19€/mois. **Éléments supprimés du plan** : migration SQLite→PostgreSQL (inutile <200 users, WAL suffit), companion port 5051 (remplacé par OVH), `boostermail_service.py` (inutile en SaaS). Manifest passe de `localhost:3443` à `api.boostermail.ai`. Cible Phase 1 : tenir **50 users actifs** avec marge ×3. Plan détaillé : `docs/plans/PLAN_SAAS.md`. **Conséquence règles** : V2 reste développé en local (`C:\EasyMail\V2\`), déployé sur OVH quand prêt. Proto (port 5050) **toujours en LECTURE SEULE** (règle absolue inchangée). |
| 26/04 | **Phase 1 SaaS terminée** | VPS OVH déployé (`51.178.162.208`), nginx reverse proxy 443→Flask 3443 HTTPS, SSL Let's Encrypt sur `api.boostermail.ai`, service systemd auto-restart. **Sécurité** : UFW (22/80/443 only), fail2ban (1 IP bannie en 17ms), SSH key-only (cloud-init par défaut). **API keys régénérées** (Anthropic + OpenAI) car les anciennes avaient été visibles dans des transcripts ; anciennes clés supprimées de la console. **Sentry** installé en free tier EU (`send_default_pii=False` pour conformité RGPD, `traces/profiles_sample_rate=0` pour rester sous quota). **Microsoft `client_secret` non régénéré** — tenant Azure non retrouvé, à faire avant Phase 2 multi-tenant. Bilan complet : `docs/sessions/SAAS_BILAN_SESSION_20260426.md`. Onboarding vivant SaaS : `docs/saas/ONBOARDING_SESSION_SAAS.md`. |
| 26/04 | **Outlook Web différé en Phase 6 (post-beta)** | Le bouton EasyMail dans Outlook Web (host `outlook.cloud.microsoft`) déclenche `displayDialogAsync` qui retourne **code 12011** : "Le domaine de la boîte de dialogue et le domaine de l'hôte du complément ne se trouvent pas dans la même zone de sécurité". Solution Microsoft : `displayInIframe: true` — appliquée et le dialog s'ouvre. **MAIS** le contenu (résumé, échéance, classement) ne se charge pas correctement dans l'iframe : erreur JS `Script error.` cross-origin masquée, body parfois vide via `messageChild`, polling en boucle des routes `/api/classement_*` sans traitement de la réponse. Décision : **ne pas bloquer la beta** sur Outlook Web — les beta-testeurs utiliseront New Outlook ou Outlook Classic. La compatibilité Outlook Web reste un objectif Phase 6 post-beta (zéro friction d'install). |
| 26/04 | **Rebrand user-visible EasyMail → BoosterMail** | 26 substitutions ciblées sur 8 fichiers, scope strict UI utilisateur : `manifest.xml` (8 — ProviderName, DisplayName, group/button labels V1.0+V1.1, buttonDesc), `popup.html` (2), `taskpane.html`/`autorun.html`/`commands.html` (1 chacun — `<title>`), `dialog.js` (1 — message body en attente), `popup.js` (9 — notifications, barre de chargement, messages erreur), `taskpane.js` (3 — notification, actionText, alert). **NON touché** : IDs internes (`easymailGroup`, `openEasyMailDialog`, `easymail_insight`...), URIs `easymail://` (couplage avec companion PyQt local New Outlook), `console.error/log` (devtools only), commentaires d'en-tête, logger Python `'easymail.v1'`. Décision validée par Yvan : "le reste on s'en fout, ca peut rester easymail en back". Commit séparé pour permettre travail parallèle session New Outlook. |
| 26/04 (PM) | **Étape 1 (ex-Phase 5) terminée — Page install + OnMessageCompose** | (1) `OnNewMessageCompose` → `OnMessageCompose` dans manifest VersionOverrides 1.1 → couvre new + reply + reply-all + forward (Mailbox 1.10+ déjà requis). `_ADDIN_VERSION` bumpé `v6` → `v7-onmessagecompose-26-04`. (2) Page HTML statique `install.boostermail.ai` créée (vanilla, single-page, responsive, 0 dépendance) avec hero + URL manifest copiable + 5 étapes visuelles + 3 cards compat + 5 FAQ. Déployée sur OVH `/var/www/install.boostermail.ai/` + nginx config dédiée (security headers, cache assets 1 an, HTML 1h). (3) DNS A record `install.boostermail.ai → 51.178.162.208` ajouté chez OVH. (4) Cert Let's Encrypt obtenu via certbot, redirect 80→443 auto. **Live** : https://install.boostermail.ai/. |
| 26/04 (PM) | **Étape 2 — Tenant Azure : nouvelle app multi-tenant** | Tentative de retrouver l'ancien tenant (client_id `209a9651-...`) infructueuse (compte Microsoft d'origine inconnu). Décision : **créer une nouvelle app Azure sur le tenant pro `groupe-bosser.fr`** (tenant ID `d66fac24-3c0a-4f23-ad6c-699325af6ec0`, propriétaire `yvan.bosser@groupe-bosser.fr`). **Avantages** : (1) tenant pro = identité commerciale crédible, (2) démarrage direct multi-tenant (cible Étape 7), (3) plus de dépendance à un compte Microsoft historique oublié. Nouvelle app **BoosterMail** créée avec types de comptes "Tout locataire Entra ID + compte personnel Microsoft" (le plus permissif, accepte BtoB + indépendants outlook.com/hotmail.com). Nouveau client_id : `42350beb-c0cb-41a0-b4cd-b869d4268206`. Redirect URI inchangée : `https://api.boostermail.ai/auth/callback`. Ancien `209a9651-...` devient orphelin (peut rester, ne reçoit plus de tokens). **Note** : warning Azure "End users cannot grant consent without verified publishers" — à résoudre via inscription MPN avant Étape 6 (AppSource). Doc complète : `docs/saas/AZURE_CONFIG.md`. |
| 26/04 (PM) | **Renumérotation phases SaaS → Étapes 1-10 chronologiques** | Le PLAN_SAAS du 25/04 numérotait les phases en ordre fonctionnel (1, 2, 3, 4, 5, 6) mais l'ordre d'exécution réel est différent. Renumérotation en **Étapes 1-10** chronologiques : É.1 = page install + OnMessageCompose (ex-Phase 5 reste), É.2 = Azure setup, É.3 = Outlook Web debug timebox 4h (ex-Phase 6), É.4 = BG webhooks (ex-Phase 3), É.5 = Infra prod (NEW : backup DB + cap API + uptime + RGPD + brand + CGU), É.6 = soumission AppSource, É.7 = Multi-tenant DB user_id (ex-Phase 2), É.8 = Beta gratuite, É.9 = Paiement Stripe (ex-Phase 4), É.10 = Publication AppSource. Mapping ancien Phase X → nouveau Étape Y dans `docs/saas/ONBOARDING_SESSION_SAAS.md` section F.2. Les noms "Phase X" restent en référence pour compatibilité avec les docs antérieurs. |
| 26/04 (PM) | **Étape 5.A — Backup DB auto quotidien** | Script `/opt/boostermail/scripts/backup_db.sh` : online backup via `sqlite3.Connection.backup()` Python (gère WAL proprement, pas de lock écriture sur `V2/boostermail.db`), compression gzip (~6x ratio, 7.6 MB → 1.2 MB), rotation 30 jours, stockage `/var/backups/boostermail/`. Cron `/etc/cron.d/boostermail-backup` quotidien 3h00 UTC. Test manuel validé : 18 tables, 106 contacts, 3042 threads, 10 échéances backupés et restaurables. Procédure de restore documentée dans onboarding section G niveau 7. Espace disque OK (46G dispo sur 49G, 30 jours = ~36 MB max). Décision SQLite Python plutôt qu'install du package `sqlite3` CLI : pas de nouvelle dépendance système, `python3` déjà installé partout. |
| 26/04 (PM) | **Étape 5.B — Cap API par user/jour (Claude + OpenAI)** | Nouveau module `V2/quota_tracker.py` (1 fichier nouveau, zéro modif des fichiers partagés avec session New Outlook). Table SQLite `api_quota` (user_id, provider, day, count, PK composite) auto-créée à la première utilisation, lazy. Hook minimal de 4 lignes au début de `core/claude_provider.py::generate()` et `core/openai_provider.py::generate()` (fichiers vérifiés non-modifiés sur master). user_id récupéré depuis Flask session (`auth_user_id`, exposé par auth_base.py). Limites par défaut : Claude 500/jour, OpenAI 200/jour (généreuses pour beta gratuite, surchargeables sans toucher au code via clé `quota.<provider>_daily` dans `config.json`). Reset auto à minuit UTC. **Fail-safe** : (1) si pas de Flask context (jobs BG, scripts CLI) → no-op, on ne quote pas les jobs internes, (2) si DB en erreur → fail-open + log, on ne bloque pas le service. **Exception levée** : `QuotaExceeded` (sous-classe `RuntimeError`). Comportement actuel : remonte en 500 standard, à améliorer plus tard pour retour HTTP 429 propre dans les routes Flask appelantes. |
| 27/04 (matin) | **Cleanup user fantôme MSAL** | Warnings `acquire_token_silent retourné None` toutes les 30s dans les logs serveur depuis le restart du 26/04 PM. Diagnostic : le `auth_token_cache` (17 740 chars en DB) contenait encore les tokens MSAL liés à l'ancien client_id orphelin `209a9651-...`. Le `auth_user_id` était lui à jour (`e8968b0b-...` du nouveau tenant `groupe-bosser.fr`). MSAL essayait de rafraîchir un token incohérent → échouait à chaque tentative auto-warmup. **Action** : `DELETE FROM settings WHERE key='auth_token_cache'` (backup DB ad-hoc avant). Re-login OAuth complet validé : flow OK, token_cache reconstruit propre 11 320 chars, `Source de donnees : Graph API` dans les logs, mail polling actif. Garde un état minimal propre (auth_user_id, email, name, last_login préservés). |
| 27/04 (matin) | **MPN (Microsoft Partner Network) — différé** | Décision de **ne pas inscrire MPN immédiatement** car la structure du Groupe Bosser n'a pas d'entité juridique « Groupe Bosser » stricto sensu. Options possibles d'éditeur : OFEC 2 (holding Paris, top de chaîne, pas vocation SaaS), OFEC (immobilier Paris, pas SaaS), 25 SCI (immobilier, pas SaaS), PDLC (Île Maurice, OFEC 2 ne détient que **10%**, faible bras de levier juridique), nouvelle SAS dédiée (recommandé long terme mais à créer). L'adresse mail unique `yvan.bosser@groupe-bosser.fr` est partagée par les 25-30 entités du groupe. Risques en cas d'inscription avec mauvaise entité : refus Microsoft (~5%), demande de complément (~25%) → 3-7 jours, ou pire = devoir migrer l'app Azure quand on inscrit la bonne entité plus tard. **Pas bloquant pour la beta** : warning Azure « publisher unverified » apparaît seulement aux users hors `groupe-bosser.fr`, et ils peuvent quand même cliquer Accepter. AppSource a 4-8 sem de validation post-soumission, on a le temps de finaliser MPN avant. **Reprise prévue** : avant l'Étape 6 AppSource, après décision business avec expert-comptable / juriste sur entité éditrice cible. Tracé dans `docs/PLUS_TARD_VF.md` (section « Différé stratégique »). |
| 27/04 (matin) | **Étape 5 close — 5.C UptimeRobot + 5.D brand + 5.E privacy + 5.F terms** | (5.C) UptimeRobot Free tier (50 monitors, 5 min, alertes mail illimitées) avec compte `yvan.bosser@gmail.com`. 2 monitors HTTP/S : `BoosterMail API` (https://api.boostermail.ai/api/warmup_status) + `BoosterMail Install` (https://install.boostermail.ai/). Test alertes validé. Petit gotcha : URL du 1er monitor saisie en doublon initialement → 404 + fausses alertes ; correction via Edit, Friendly name renommé. (5.D) Brand check : aucune référence "EasyMail" user-visible (toutes les refs restantes sont scope interne — commentaires source, console.error, IDs de fonctions). 1 micro-incohérence couleur sur bouton `btnActiverStandard` (#0078d4 au lieu de #0F6CBD), différée. (5.E) `V2/install_landing/privacy.html` — politique de confidentialité 10 sections RGPD-compliantes, déployée sur `https://install.boostermail.ai/privacy.html`. Bandeau jaune "Document en cours de finalisation" + placeholder "Entité éditrice à finaliser" pour ne pas tromper avant validation juridique + décision MPN. (5.F) `V2/install_landing/terms.html` — CGU draft 17 sections, idem bandeau draft. Footer index.html mis à jour avec liens. |
| 27/04 (PM) | **Pivot stratégique : OVH = source de vérité unique** | Décision Yvan : à partir du 27/04 PM, **OVH est la version officielle de BoosterMail**. Plus de WIP local persistant, plus de divergence local↔serveur. Toutes les modifications (UX, optims, fixes) sont désormais déployées sur OVH dès qu'elles sont commitées en git. Conséquence : Yvan utilise BoosterMail au quotidien depuis OVH (pas de v locale en parallèle), valide le produit en conditions réelles avant d'attaquer le multi-tenant (Étape 7) qui est repoussé jusqu'à ce que la version mono-user soit nickel sur OVH. Action de mise en œuvre 27/04 PM : (1) la session Outlook a commité ses 3 batchs WIP (291c58b frontend + 0fa4ec6 backend + abb27e9 docs) ; (2) la session SaaS a mergé `claude/angry-ishizaka-26efe7` dans master pour consolider les 10 commits SaaS + résoudre 7 conflits (manifest auto-merge, autorunshared.js _ADDIN_VERSION bumpé `v8-merge-saas-outlook-27-04`, app_plugin/dialog : --ours = master, docs : fusion manuelle des contenus complémentaires) ; (3) déploiement code master → OVH ; (4) bascule DB locale (nettoyée par session Outlook : profils contacts corrigés, doublons supprimés) → OVH. |
| 27/04 (PM) | **Fix bouton BoosterMail mort sur New Outlook + Pattern #18 cache WebView2** | Premier sujet de la session « New Outlook via OVH ». Bug critique : depuis le pivot SaaS, le bouton BoosterMail dans New Outlook desktop ne déclenchait rien (POST `/api/companion/open_dialog_native` → 503 sans fallback `displayDialogAsync`). **Fix** : suppression complète du fast path companion + de la fonction `_openDialogPlatformRouted` dans `autorunshared.js` ; toutes les plateformes (Classic, New Outlook, Web) passent désormais par `displayDialogAsync` directement. Bump `_ADDIN_VERSION` → `v9-fix-newoutlook-button-27-04`. **Découverte structurelle Pattern #18** : New Outlook desktop (WebView2 hosted) **ignore les headers HTTP `must-revalidate`** et garde un cache disque permanent dans `%LOCALAPPDATA%\Microsoft\Olk\EBWebView`. Pour invalider : (a) `Cache-Control: no-store, no-cache, must-revalidate, max-age=0` + `Pragma: no-cache` + `Expires: 0` sur tous les `.js`/`.html`/`.css` du plugin (route `/plugin/<filename>`), (b) cache busting URL dans `autorun.html` (`<script src="autorunshared.js?v=v9-...">`), (c) procédure de purge `EBWebView` documentée pour la première fois (one-shot pour repartir propre). La promesse antérieure « bumper `_ADDIN_VERSION` invalide le cache 304 » documentée dans onboarding était fausse — corrigée. **Cleanup associé** : retrait `'open_dialog_native'` de la whitelist `_COMPANION_ALLOWED` ; ajout du guard `not graph` sur le fallback companion de l'onboarding (évitait un timeout 5s en SaaS). Audits préventifs livrés : Pattern #15 (4 sites à inspecter en backlog), Pattern #17 (couvert), bugs UI bilan SAAS PM (déjà fixés en code mais non visibles côté Yvan à cause du Pattern #18). Pattern #18 + 3 invariants I-CACHE-01/02/03 ajoutés dans `audit/`. |
| 27/04 (PM tardif) | **Cycle audit kit complet + tech debt + consolidation PLUS_TARD_VF** | Suite de la session « New Outlook via OVH » jusqu'au soir : (1) **8 audits Workflow 4/2 du menu de 10** clos en application stricte du `audit/PLAYBOOK.md` : #2 Graph 400 conversationId (89 erreurs/jour → 0, retrait `$orderby` + tri Python), #3 Cross-user SaaS readiness (rapport livré : 22 caches mono-user à isoler pour Étape 7 multi-tenant, plan de migration ready), #4 except: pass (constat 71 occurrences → fix dédié 1-2h hors scope), #5 Phase 1 IMID canonical (couvert par fix Pattern #15), #6 Prompt injection (`_build_prompt` était NON protégé — fix structurel + invariant **I-SEC-06** ajouté), #9 Slow paths (toutes routes < 50 ms, OK), #10 Code mort (4 caches dead retirés). (2) **Pattern #15 fix 4 sites** : `_preload_neighbors`, `api_event_message_read` (CRITIQUE — chaque ouverture mail), Mode Dégradé companion, `_prescan_summary` — tous corrigés (cohérence canonique IMID). (3) **HTTP 429 propre** : `@errorhandler(QuotaExceeded)` global + SSE event `quota_exceeded` UX clair côté frontend. (4) **Optim contact_search** : nouveau endpoint `/api/contact_search?q=` debounced 150ms côté dialog.js, remplace le pré-load 187 KB de `/api/contact_profiles` (gain ~180 KB par clic, ~100 ms parse JSON). (5) **Tech debt tier 1** : locks cohérents `_current_mail_data` + `_warmup_cache`, log level `acquire_token_silent` WARNING → DEBUG, cleanup `_checkSpeculativeCache` deprecated. (6) **12 contacts humains analysés** via `POST /api/analyze_contact` ($0.30, 9 profils créés en 60s). (7) **Documentation** : consolidation `PLUS_TARD_VF.md` (3 anciens fichiers archivés avec bandeau), 6 rapports d'audit dans `audit/rapports/`, ANOMALIES_RECURRENTES + INVARIANTS + ONBOARDING + SOMMAIRE_DETAILLE à jour, bilan de session complet. État OVH : service active, 0 erreur, latence routes < 50 ms, cache HTTP différencié (no-store HTML / immutable 5 ans JS-CSS). *(Cumul commits journée → voir entrée 27/04 (soir) ci-dessous.)* |
| 27/04 (soir) | **Kit fin de session opposable (Workflow 7/8 + I-SESS-01 à 04)** | Demande Yvan en fin de journée : « créer une procédure complète de fin de session et d'ouverture d'une nouvelle session, comme le kit audit ». Approche minimale (3 fichiers étendus + 1 script créé) : (1) `audit/PLAYBOOK.md` : Workflow 7 (clôture, déclencheur Yvan = `kit fin de session`) + Workflow 8 (ouverture, déclencheur = collage du PROMPT_REPRISE), engagement opposable, étapes strictes, anomalies à éviter incluant le paradoxe d'auto-référence des chiffres. (2) `audit/INVARIANTS.md` : Catégorie 13 « État de session / cohérence documentaire » — **I-SESS-01** (`git status --short` vide en clôture), **I-SESS-02** (aucune ref obsolète vers `PLUS_TARD.md` sans `_VF` ou `TODO_SESSION_SUIVANTE` qualifiée « état courant » dans docs vivants), **I-SESS-03** (top commit hash dans PROMPT_REPRISE ∈ 20 derniers commits), **I-SESS-04** (chiffre « N commits » cohérent entre tous docs vivants). (3) `audit/tests/cloture_check.sh` : script bash de vérif auto des 4 invariants, exit 0 obligatoire avant déclaration session close. (4) `PROMPT_REPRISE` : sections OUVERTURE/FIN reformulées en refs Workflow 7/8. **Validation grandeur nature** : 1er run du script a détecté 3 anomalies réelles (modifs non commitées, ref `TODO_SESSION_SUIVANTE` qualifiée « état courant » dans `PLAN_ACTION_PHASE_2.md`, chiffres « 29 » vs « 30 » incohérents) — toutes corrigées avant validation. Cumul session 27/04 : **~32 commits**. Workflow utilisateur : Yvan tape `kit fin de session` → Claude exécute Workflow 7 ; nouvelle session : Yvan colle `PROMPT_REPRISE` → Claude exécute Workflow 8. |
| 28/04 (matin) | **3 sujets PLUS_TARD_VF traités end-to-end** | #3 signature personnalisée par contact (commits `d9a454d` + `1126e80`), #4 wording transparent classement « pas de suggestion » (`fef423f`), #2 instrumentation pipeline templates (`86d0e60`). Top commit cloture `9e2c35e`. Audit kit Workflow 2 systématique. Bilan dédié `OUTLOOK_BILAN_SESSION_20260428.md`. |
| 28/04 (PM) | **Outlook Web fonctionne — bug iframe content du 26/04 disparu** | Découverte début de session : BoosterMail tournait déjà sur Outlook Web (sideloadé il y a un certain temps), tout le pipeline opérationnel (`dialog_init`, `instant_reply`, classement, échéance — tous 200 OK). Le bug iframe content du 26/04 a été résolu indirectement par les fixes du 27/04 (Pattern #18 cache no-store + headers HTTP corrects). **Étape 3 du planning SaaS** (timebox 4h debug iframe) → considérée close. **Conséquence** : on peut viser Outlook Web ET New Outlook desktop simultanément dans le scope « New Outlook nickel » (avant : web différé Phase 6 post-beta). |
| 28/04 (PM) | **Refonte UI dialog 80%** | Reformulation header sur les 2 plateformes : (1) déplacement bouton trombone 📎 + chips R/S/H sur la même ligne que les boutons mode (Répondre/Rep. à tous/Transférer) ; (2) suppression du champ « Instructions optionnel » devenu obsolète (le refine en bas de l'éditeur fait le même job, le textarea fieldBrief reste caché en `display:none` pour rétro-compat dialog.js — 6 références) ; (3) suppression du badge bleu « Pré-générée il y a X j » au-dessus de l'éditeur (Option A : suppression complète y compris pour les templates avec confidence — décision Yvan car l'info ne sert pas à l'utilisateur, encombre visuellement). Gain hauteur utile : ~64-70 px cumulés. Versions : autorunshared.js v17, dialog.css v19, dialog.js v18. |
| 28/04 (PM) | **Pattern #19 — Convergence Microsoft New Outlook desktop ↔ Outlook Web** | Découverte structurelle : sur le build `OneOutlook/1.2026.420.300` (daté 20/04/2026, capturé via `addin_debug.log`), Microsoft a uniformisé le rendu de `displayDialogAsync` entre New Outlook desktop et Outlook Web — `displayInIframe: true` affiche désormais le même chrome iframe (titre add-in + croix + corniche) partout, alors qu'avant c'était une fenêtre WebView2 native pleine sur desktop. Notre manifest XML inchangé (vérifié `27/04 07:12` last modified sur OVH), donc 100% côté Microsoft. **Conséquence stratégique** : aligner notre code sur le comportement Outlook Web par défaut, étendre les règles CSS compactes (auparavant uniquement `html.platform-web`) à `html.platform-newOutlook`. Tester systématiquement sur les 2 plateformes à chaque modif UX dialog. Documenté dans `audit/ANOMALIES_RECURRENTES.md`. |
| 28/04 (PM) | **Option E v17 retenue — chrome Microsoft centré + 80×80 + compactage CSS étendu** | Après plusieurs tests itératifs : (a) v15 plein écran 100×100 `displayInIframe: false` rejeté car trop intrusif (perte du contexte Outlook visible derrière, 3 cartes du bas coupées), (b) test `displayInIframe: false` à 90×90 rejeté car popup Edge bloquée + code 12011 (zone de sécurité Microsoft), (c) Option E retenue : `displayInIframe: true` partout (chrome Microsoft revient mais centré naturellement par Microsoft) + 80×80 (marge autour pour voir Outlook = contexte rassurant) + compactage CSS étendu à `html.platform-newOutlook` (~16 px gagnés sur le header). État final validé par Yvan : « la meilleure version de la session, je suis très très très content du résultat ». **Stratégie globale** : cesser de combattre Microsoft, accepter le chrome iframe et compenser ailleurs. |
| 28/04 (PM) | **Welcome wizard 3 étapes consolidées avant Étape 8 Beta — sujets #11, #12, #13** | Plan d'onboarding produit sur `api.boostermail.ai/welcome` (NOUVELLE page à créer en SaaS) en 3 étapes guidées de 2 minutes : étape 1 = autoriser les popups Edge (sujet #11 fallback intelligent au cas où), étape 2 = activer le placement intelligent multi-écrans via Window Management API (sujet #12), étape 3 = épingler BoosterMail dans la barre d'actions Outlook Web (sujet #13 — politique Microsoft sideload qui relègue les add-ins customs au launcher). Architecture SaaS-correcte (post-pivot 27/04 : plus de phase « install » classique, juste sideload + welcome). Effort total session welcome dédiée : 8-9 h. À attaquer juste avant Étape 8 Beta (5-10 testeurs externes). Documenté en détail dans `docs/PLUS_TARD_VF.md` sections #11/#12/#13. |
| 28/04 (PM) | **Fix bouton « Relire et envoyer » grisé sur cache HIT instant_reply** | Bug détecté en début de session sur Outlook Web : le bouton btnSend restait grisé après affichage d'une réponse pré-générée (cache HIT). Diagnostic : le HTML déclare `btnSend disabled` par défaut (l. 294 dialog.html), `_onGenerationDone()` (l. 2040) l'active après une vraie génération streaming, **mais le path cache HIT instant_reply (l. 1685-1707) ne passait pas par cette activation**. Sur Outlook Web, un effet de bord du browser (re-render via assignation `editor.innerHTML`) le réactivait accidentellement, mais sur New Outlook desktop (WebView2 isolé), pas de re-render → bouton restait grisé. **Fix** : ajout explicite de `document.getElementById('btnSend').disabled = false` dans le bloc cache HIT, après `_showInstantReplyBadge`. Comportement uniforme web ↔ desktop. dialog.js v18. **Lien probable avec « 0 envoi BoosterMail/12j » du diagnostic du 28/04 matin** : Yvan ne pouvait pas envoyer via BoosterMail tant que le bouton restait grisé. À surveiller post-migration Coaxis. |
| 28/04 (PM) | **🔥 Migration Coaxis terminée — débloqué OnMessageCompose pour la prochaine session (#14)** | Annoncé par Yvan en fin de session 28/04 PM : la mailbox `yvan.bosser@groupe-bosser.fr` est définitivement migrée des serveurs Coaxis vers Microsoft 365 cloud. Conséquence technique majeure : tous les events Office.js et Graph fonctionnent désormais à 100% (avant : `MailboxInfoStaleException`, accès Graph limité). **Sujet #14 PLUS_TARD_VF débloqué** : auto-ouverture popup BoosterMail au clic « Répondre » Outlook (OnMessageCompose). Le handler `onNewMessageComposeHandler` existe déjà dans `autorunshared.js` ligne 481 et est correctement déclaré dans le manifest, mais son comportement actuel (notify backend pour popup PyQt locale supprimée au pivot SaaS) est devenu mort. À adapter pour ouvrir directement la popup BoosterMail via `displayDialogAsync`. **Effort estimé** : 6-7 h en session dédiée. **Décision Yvan** : c'est le **premier sujet à attaquer le 29/04** (prochaine session). Effet WOW majeur : l'utilisateur clique « Répondre » comme dans n'importe quel client mail, BoosterMail s'ouvre automatiquement avec la réponse pré-générée. Détail complet dans `docs/PLUS_TARD_VF.md` section #14. |
| 29/04 (matin) | **Sujet #14 OnMessageCompose — vision auto-ouverture popup ABANDONNÉE, Option A2 bandeau passif livrée v20** | Audit pré-code via kit audit Workflow 4 a révélé **3 limitations Microsoft cumulées** rendant la vision originale (auto-ouverture à 0 clic) techniquement impossible : (1) `displayDialogAsync` listée dans **Unsupported APIs** des event-based handlers Outlook (doc Microsoft Learn « Activate add-ins with events », mise à jour 21/04/2026 ; issue OfficeDev/office-js#3085 « by design » depuis 2023), (2) `actionType` du bouton actionable d'`InsightMessage` cadenassé sur **`ShowTaskPane` uniquement** (`Office.MailboxEnums.ActionType` n'a qu'un seul field, vérifié sur Mailbox 1.10 → 1.15), (3) **cold start runtime event-based** 5-15 sec sur 1er trigger session (instantané ensuite), inhérent à l'architecture Microsoft. Combiné à l'interdiction taskpane (`feedback_taskpane_interdit.md`), aucune voie technique pour atteindre 0-clic sans validation AppSource. **Pivot Option A2** : bandeau passif `InformationalMessage` non-persistent au compose, dirige vers le bouton ruban BoosterMail. Wording final v20 : « 🚀 BoosterMail : votre réponse est prête — cliquez sur l'icône BoosterMail ». Aucun gain de clic vs bouton ruban classique, juste plus de visibilité produit. **Code Phase 2 toggle settings** (v21) écarté — documenté dans `audit/rapports/2026-04-29_bilan_intermediaire_oncompose_banner.md` au cas où on voudrait le ressortir plus tard. **Documentation structurelle** : Pattern #20 « OnMessageCompose : 3 limitations Microsoft cumulées » + invariants `I-EVENT-01` + `I-EVENT-02` ajoutés. Investigation pinning Microsoft livrée en parallèle (sub-agent dédié) : politique de relégation `More apps` identique New Outlook desktop ↔ Outlook Web (Microsoft Q&A 04/09/2025), aucun attribut manifest XML pour forcer pinning, mécanisme manuel `clic-droit More apps → Pin`, AppSource ou Centralized Deployment seules voies garanties. Welcome wizard #11+#12+#13 reste à attaquer avant Étape 8 Beta. Rapports : `audit/rapports/2026-04-29_audit_approche_OnMessageCompose_InsightMessage.md` + `audit/rapports/2026-04-29_bilan_intermediaire_oncompose_banner.md`. |
| 29/04 (mi-journée) | **Pivot stratégique vers Étape 7 multi-tenant** | Après 4h sur le sujet #14 (audit + Phase 1 v20 + investigation pinning + tentative Phase 2 v21 écartée), Yvan tranche : « le travail sur le bandeau est fini, on attaque le multi-tenant ». Le vrai levier produit aujourd'hui = pouvoir distribuer à plusieurs beta-testeurs simultanément. Plan d'attaque (basé sur `audit/rapports/2026-04-27_audit_cross_user_saas_readiness.md`) : (Phase 0) cleanup 4 dead code caches identifiés (`_attachment_cache`, `_echeance_post_send_cache`, `_classification_post_send_cache`, `_pj_classification_post_send_cache`), (Phase 1) helper `V2/user_scoped_cache.py` avec `get_user_cache(cache_name, user_id)` + décorateur `@require_user`, (Phase 2-3) migration des 22 caches mono-user identifiés en `dict[user_id, dict[key, value]]`, (Phase 4) tests bout-en-bout 2 comptes Microsoft, (Phase 5) cleanup BG périodique. Effort total estimé 1.5 jour. Démarrage immédiat 29/04 PM par phases 0-1. v20 reste sur OVH (visibilité produit). |
| 29/04 (PM tardif, finalisation) | **Étape 7 multi-tenant TERMINÉE — 22/22 caches (100%) + cleanup BG users inactifs** | Finalisation complète Étape 7 SaaS. (1) **Migration `_warmup_done` + `_warmup_progress`** : `_warmup_progress` passe en `UserScopedDict('warmup_progress')` (transparent, sites de lecture utilisaient déjà `.get()` avec defaults). `_warmup_done` (boolean global) refactor en wrappers `_is_warmup_done()` / `_mark_warmup_done()` qui accèdent à la clé `'done'` du sub-cache `warmup_progress`. ~10 sites modifiés (3 globals retirés, 2 réassignations remplacées par `_mark_warmup_done(True)`, 5 lectures par `_is_warmup_done()`). Décision sémantique : warmup state par-user (chaque user a son warmup serveur cohérent avec sa Graph mailbox). (2) **Cleanup BG périodique** `_multi_tenant_cleanup_loop` : thread daemon `mt-cleanup` qui scanne 1×/24h les users inactifs > 30j (critère timestamp `_my_email` sub-cache) et purge tous leurs sub-caches via `purge_user_caches(user_id)`. Skip 'default' (bridge mono-user jamais purgé). Fail-safe si helpers indisponibles. Premier scan après 1h pour laisser le système démarrer. Économise mémoire RAM en multi-user (5-10 beta-testeurs Phase 8 SaaS). En mono-user actuel, tourne mais ne purge rien (Yvan utilise BoosterMail au quotidien, son timestamp _my_email rafraîchi régulièrement). (3) **Validation prod OVH 10:08** : service active, warmup OK, `done:true` retourné par `/api/warmup_status` (preuve que `_is_warmup_done()` fonctionne via le sub-cache user-scoped). 0 erreur. (4) **`@require_user`** : décorateur déjà déclaré dans `V2/user_context.py` (commit 5975df7), code prêt à utiliser. Volontairement NON appliqué aux routes sensibles aujourd'hui : nécessite un 2e compte Microsoft pour tester sans risquer de casser la session de Yvan. À faire en session dédiée future avec un user test. (5) **Documentation** : invariant I-MT-01 mis à jour (22/22 caches), HISTORIQUE_DECISIONS et PLUS_TARD_VF Étape 7 marquée TERMINÉE. **Récap journée 29/04 — 11 commits Étape 7 + 1 commit sujet #14 = 12 commits master au total**. |
| 29/04 (PM tardif, suite) | **Étape 7 multi-tenant — finalisation 20/22 caches migrés (91% couverture) + invariant I-MT-01** | Suite des migrations majeures + bridge DB. Migration en bloc des 14 caches restants en 4 commits atomiques (`4818c68`, `c2c102f`, `fcf4eaa`) : (1) **7 caches secondaires** `_mail_preview_cache`, `_c_keyword_cache`, `_mail_open_counter`, `_last_generate_times`, `_echeance_pre_scan_cache`, `_pj_text_cache`, `_last_proposed` — déclarations remplacées par `UserScopedDict`, ~24 sites de lecture/écriture transparents grâce au proxy. (2) **3 caches one-shot dict** `_classify_momentum`, `_learning_priorities_cache`, `_contacts_recalib_progress` — refactor des réassignations globales (`global _xxx; _xxx = {...}`) en `clear() + update()` pour préserver le proxy. Sites de lecture adaptés avec `.get(key, default)` pour gérer l'init initial à vide. (3) **5 slots/state** `_current_mail_data`, `_current_compose_data` (BG poll loop + route compose), `_sent_requests` (idempotence requêtes), `_post_send_cache`, `_post_send_timestamps` — réassignations remplacées par clear+update, retrait des `global` devenus inutiles. (4) **`_outlook_folders_cache`** (TTL 5min) — refactor list+float globaux en sub-cache user-scoped `{list, ts}` via `get_user_cache('outlook_folders', user_id)`, lock `_outlook_folders_lock` conservé pour double-check anti rate-limit Graph 429. **Validation prod OVH 09:55-09:57** : `[reply_cache] Persist 60 entrées sur 1 user [e8968b0b-6fc=60]`, `[cache] Prefetch V2 sauvegardé : 98 entrées sur 1 user [e8968b0b-6fc=98]`, `[reply_cache cohesion] 6 entrée(s) purgée(s) pour user e8968b0b-6fc`, `[speculative] Prefetch ready in 0.00s` (HIT instant prouve que BG et routes Flask convergent vers le même sub-cache via bridge DB). **0 erreur sur les migrations** (les Graph 400 observés sont des bugs métiers pre-existants non liés). **Nouvel invariant I-MT-01** ajouté dans `audit/INVARIANTS.md` pour documenter le pattern multi-tenant user-scoped (test grep, architecture, cas spéciaux réassignation/persistance, liste des caches migrés). **2 caches non migrés restants** : `_warmup_done` (boolean global, état serveur warmup), `_warmup_progress` (one-shot dict avec init values complexes) — peu critiques en mono-user, à refactoriser quand multi-user vraiment activé. **Prochaines étapes (sessions futures, ~2-3h)** : décorateur `@require_user` appliqué aux ~20 routes sensibles (RISQUE de casser sans tests 2 comptes Microsoft, à faire avec un user test) + tests bout-en-bout 2 comptes simultanés + cleanup BG périodique purge users inactifs >30j. **Récap journée 29/04** : 9 commits multi-tenant Étape 7 (`8f64783` → `fcf4eaa`) + 1 commit sujet #14 OnMessageCompose (`910ec91`). |
| 29/04 (PM tardif) | **Étape 7 multi-tenant — bridge DB user_id + migration `_warmup_cache` + `_prefetch_cache`** | Suite directe de la migration `_reply_cache` (commit `3d60954`). **Bridge DB ajouté** dans `V2/user_context.py` (commit `b9b2c11`) : `get_current_user_id()` enrichi d'un 3e niveau de fallback qui lit `auth_user_id` depuis la table `settings` quand pas de Flask context (BG threads). Cache 60s en mémoire pour éviter SQLite hits massifs. Garantit que **BG cont-spec et routes Flask convergent vers le MÊME user_id en mono-user** (sans bridge, BG écrivait dans `'default'` mais routes Yvan lisaient dans son `auth_user_id` réel → MISS systématique, ~1.77$ regen). **Migration `_warmup_cache` + `_prefetch_cache`** : 1 ligne par cache (`_xxx_cache = UserScopedDict('xxx')`). Pour `_prefetch_cache` qui est persistent disque (TTL 48h), les fonctions `_save_prefetch_cache` + `_load_prefetch_cache` adaptées format v2 (`entries_per_user`) avec migration legacy v1→v2 transparente + migration `default → user_id réel` au load. **Validation prod OVH 09:47** : 59 entrées `_reply_cache` + 98 entrées `_prefetch_cache` toutes migrées vers `e8968b0b-6fc` (auth_user_id Yvan) sans perte, 0 erreur, service active, warmup OK. **3 caches majeurs migrés au total** (`_my_email_cache` + `_reply_cache` + `_warmup_cache` + `_prefetch_cache`). **Reste à migrer** (sessions futures, ~3-4h) : 18 caches secondaires (`_mail_preview_cache`, `_classify_momentum`, `_pj_text_cache`, `_last_proposed`, etc.) + décorateur `@require_user` sur routes sensibles + tests bout-en-bout 2 comptes Microsoft + cleanup BG périodique purge users inactifs >30j. |
| 29/04 (PM) | **Étape 7 multi-tenant — helpers + migration `_reply_cache` (LE plus critique) en production** | Trois livrables en ~1h chacun, déployés et validés en production OVH (service active, 0 erreur, 59 entrées migrées sans perte) : (1) **`V2/user_scoped_cache.py`** (commits `8f64783` puis enrichi) — helpers `get_user_cache`, `iter_user_caches`, `get_all_user_ids`, `replace_user_caches` + classe `UserScopedDict` (proxy dict transparent qui résout user_id via Flask context à chaque accès). 20 tests inline validés (isolation cross-user, idempotence, thread-safety 50 écritures concurrentes, proxy avec Flask context). Cf code module pour API complète. (2) **`V2/user_context.py`** (commit `5975df7`) — `get_current_user_id()` (résout depuis `request.auth_user_id` puis `session['auth_user_id']`, fallback `''` hors Flask context) + décorateur `@require_user` (strict 401 si pas de session). 7 tests inline validés. POC migration `_my_email_cache` via `_get_my_email()` (1 fonction, 5 call-sites transparents). (3) **Migration `_reply_cache`** — le plus gros chantier : `_reply_cache = UserScopedDict('reply')` au lieu d'un dict global. Les ~95 call-sites (lecture/écriture dict standard) continuent de fonctionner sans modification grâce au proxy. **Persistance disque migrée v1 → v2** : ancien format `{entries: {mid: entry}}` → nouveau `{format_version: 2, entries_per_user: {user_id: {mid: entry}}}`. Migration legacy automatique au load (entrées existantes assignées à user_id `'default'`). Re-persist immédiat post-load pour migrer le fichier sur disque. **3 threads BG adaptés** : `_reply_cache_cohesion_refresh` itère via `iter_user_caches('reply')` (chaque user purgé indépendamment, log `purged for user XXX`), `_reply_cache_safety_net_loop` itère pour purger les entrées > 4 semaines cross-user, `_persist_reply_cache` collecte cross-user pour serialiser. **Validation prod OVH 09:33** : 59 entrées legacy migrées en `'default'`, format v2 sur disque confirmé, 4 orphelines purgées par cohesion refresh à 09:33:48 (logs typés `purgée(s) pour user default`), 0 erreur. **Yvan continue à utiliser BoosterMail normalement** (BG threads sans Flask context résolvent à `'default'` → comportement strictement identique au mono-user d'avant). **Architecture prête pour le multi-user** : à terme, chaque future beta-testeur aura son sub-cache isolé, zéro fuite cross-user, drafts pré-générés strictement isolés (point critique sécurité). **Reste à migrer en sessions futures** : `_warmup_cache` + `_prefetch_cache` (~22 sites combinés, ~1.5h) puis 19 caches secondaires (~30-40 sites, ~2h) + tests bout-en-bout 2 comptes Microsoft + cleanup BG périodique purge users inactifs >30j. Effort restant ~6-7h. Audit cross-user de référence : `audit/rapports/2026-04-27_audit_cross_user_saas_readiness.md`. |
